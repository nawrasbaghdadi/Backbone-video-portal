describe("loginAattempt", function() {
  var j;
  it("j not to be null", function() {
    expect(j).not.toBe(null);
  });
});

describe("ratingChanges", function() {
  var j;
  it("j not to be null", function() {
    expect(j).not.toBe(null);
  });
});


describe("logout", function() {
  it("logout to logout user", function() {
    expect(true).toBe(true);
  });
});

describe("login", function() {
  var videoView;
  var loginView;
  it("videoView not to be null", function() {
    expect(videoView).not.toBe(null);
  });
  it("loginView not to be null", function() {
    expect(loginView).not.toBe(null);
  });
});
describe("videos", function() {
  var infoView;
  var navView;
  var videoCoolleciton;

  it("infoView not to be null", function() {
    expect(infoView).not.toBe(null);
  });
  it("navView not to be null", function() {
    expect(navView).not.toBe(null);
  });
  it("videoCoolleciton not to be null", function() {
    expect(navView).not.toBe(null);
  });
  
});

